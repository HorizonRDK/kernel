ifdef ELP_SPACC_HW_PROJ_NUM
EXTRA_CFLAGS += -DELP_SPACC_HW_PROJ_NUM=${ELP_SPACC_HW_PROJ_NUM}
endif

ifdef PDU_USE_PCI
EXTRA_CFLAGS += -DDO_PCIM=1
ifdef PDU_PCI_DID
EXTRA_CFLAGS += -DPDU_PCI_DID=${PDU_PCI_DID}
endif
endif

ifdef PDU_BASE_ADDR
EXTRA_CFLAGS += -DPDU_BASE_ADDR=${PDU_BASE_ADDR}
endif

ifndef ELP_TYPES_VERSION
EXTRA_CFLAGS  += -DELP_TYPES_VERSION=1
endif

ifdef PDU_LITTLE_ENDIAN
EXTRA_CFLAGS += -DSDK_ENDIAN_LITTLE=1
else
EXTRA_CFLAGS += -DSDK_ENDIAN_BIG=1
endif

